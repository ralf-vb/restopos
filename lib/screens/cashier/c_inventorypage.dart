import 'package:flutter/material.dart';

class c_inventorypage extends StatelessWidget {
  const c_inventorypage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}