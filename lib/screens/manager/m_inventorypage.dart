import 'package:flutter/material.dart';

class m_inventorypage extends StatelessWidget {
  const m_inventorypage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}