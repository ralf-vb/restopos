import 'package:flutter/material.dart';

class m_viewreportspage extends StatelessWidget {
  const m_viewreportspage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
    );
  }
}